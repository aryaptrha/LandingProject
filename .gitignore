# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local
.env
.env.*
!.env.example

# Wrangler local secrets (worker vars for `wrangler dev`)
.dev.vars
.dev.vars.*
# ...but the template is committed, since it names the vars without their values.
!.dev.vars.example

# Wrangler's local state: the simulated D1 database and KV namespace that
# `wrangler dev` and `wrangler d1 migrations apply --local` write to. Machine-local
# scratch data, and it grows every time you run the dev server.
.wrangler/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

.eslintcache

# Cypress
/cypress/videos/
/cypress/screenshots/

# Vitest
__screenshots__/

# Vite
*.timestamp-*-*.mjs

# Claude Code
CLAUDE.md
